<!DOCTYPE html>
<html lang="sv">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title><%= assigns[:page_title] || "ByggApp" %></title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <%= csrf_meta_tag() %>
    <script src="https://kit.fontawesome.com/de245c9f2b.js" crossorigin="anonymous"></script>
  </head>
  <body class="bg-gray-100">
    <header class="bg-blue-900 text-white pb-56">
      <section class="container mx-auto px-2">
        <nav role="navigation" class="border-b border-blue-700 flex justify-between pt-3">
          <div class="flex  space-x-4">
            <%= nav_link @conn, gettext("Requests"), to: Routes.job_request_path(@conn, :index) %>
            <%= nav_link @conn, gettext("Jobs"), to: Routes.job_path(@conn, :index) %>
          </div>
          <div class="cursor-pointer relative group">
            <div class="py-2 border-b-4 border-transparent group-hover:border-orange-600 pr-4">
              <%= assigns.current_user.company %>
              <i class="fas fa-ellipsis-v ml-4"></i>
            </div>
            <div class="absolute hidden group-hover:flex flex-col shadow inset-x-0">
              <%= link gettext("Settings"), to: Routes.user_settings_path(@conn, :edit), class: "px-2 py-2 bg-white text-blue-800 hover:bg-gray-200 hover:no-underline" %>
              <%= link gettext("Logout"), to: Routes.user_session_path(@conn, :delete), method: :delete, class: "px-2 py-2 bg-white text-blue-800 hover:bg-gray-200 hover:no-underline" %>
            </div>
          </div>
        </nav>
      </section>
    </header>
    <main role="main" class="container mx-auto px-2 pb-4 -mt-48">
      <%= page_header(assigns[:page_header]) %>

      <%= @inner_content %>

    </main>
    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
